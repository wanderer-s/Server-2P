let local = 'http://localhost'
let web_server_url = `${local}:3001`
let game_mole_url = `${local}:3009`
let chat_server_url = `${local}:3002`


module.exports = {
  web_server_url,
  game_mole_url,
  chat_server_url,
};
